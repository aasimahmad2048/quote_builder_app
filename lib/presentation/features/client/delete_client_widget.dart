import 'package:flutter/material.dart';

class DeleteClientWidget extends StatelessWidget {
  const DeleteClientWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
